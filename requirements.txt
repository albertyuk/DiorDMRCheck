fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
jinja2>=3.1,<4
python-multipart>=0.0.9
openpyxl>=3.1,<4
# Archive metadata is attacker-controlled OOXML; use hardened streaming XML
# parsing for validation before openpyxl sees a workbook.
defusedxml>=0.7,<1
# pillow: without it openpyxl silently drops embedded images/shapes when
# re-saving the annotated workbook, breaking the "A-R untouched" contract.
pillow>=10
# pandas is part of the specified deployment dependency set; the pipeline
# itself reads xlsx via openpyxl directly.
pandas>=2.2,<3
rapidfuzz>=3.9,<4
pypinyin>=0.51
httpx>=0.27,<1
anthropic>=0.40
pydantic>=2.7,<3
# KOL efficiency report: native editable charts in the .pptx, patched at the
# chart-XML level (donut hole, per-point label colors, sliver callouts).
python-pptx>=1.0,<2
lxml>=5
